# 变更记录与文档维护规范

## 1. 文档维护规范（每次改代码都要执行）

改完代码，在提交前按这四步更新文档。**这是整套文档能省 token 的前提**：文档一旦落后，下一个 AI 只能重新读源码，前面的功夫全废。

### 第一步：本篇加一条记录

在 §2 顶部按倒序加一条，格式：

```markdown
### YYYY-MM-DD 一句话标题

- 改了什么：具体到文件和函数
- 为什么：动机或修的什么问题
- 牵连：新增/改动的接口、字段、类名、配置
- 已更新文档：06 §2、04 §2
```

“牵连”那行最值钱，写清楚别人会被什么绊到。

### 第二步：按改动类型更新对应文档

| 改动类型 | 必须同步的文档 |
| --- | --- |
| 加/删/改文件 | [06-文件地图.md](06-文件地图.md) 的表格（作用、关键符号、行数） |
| 加/改接口 | [04-接口与数据字典.md](04-接口与数据字典.md) 的清单和字段 |
| 加/改设置项 | 04 的设置字段表 |
| 改数据库表结构 | 04 的数据字典；若影响 `messages` 表，检查 07 §3 提到的 `cleanupFailedAttempt` |
| 改流程、数据流、SSE 协议 | [02-架构与数据流.md](02-架构与数据流.md) |
| 改 Electron、IPC、注入脚本、打包 | [07-桌面端与打包.md](07-桌面端与打包.md) |
| 新踩到的坑 | [05-故障排查.md](05-故障排查.md)，坑够大就同时写进 [00-AI-上手索引.md](00-AI-上手索引.md) §3 |
| 新增一类常见改法 | [03-常见修改操作.md](03-常见修改操作.md) |
| 目录结构变了 | 06 §1 和根 `README.md` |

### 第三步：检查 00 的路由表

新增功能模块要在 [00-AI-上手索引.md](00-AI-上手索引.md) §2 加一行，否则下一个 AI 找不到入口，只能全库搜。

### 第四步：只改动过的地方，不要重写文档

文档是增量维护的。别因为“顺手整理”把整篇重写——那会丢掉前人踩坑记下的细节。行号偏移了顺手修一下就好。

### 写文档的取舍

- 写**为什么**和**牵连**，不写源码能直接看出来的东西。函数签名不用抄，加一句“它同时会清空 `active_leaf_id`”才有价值。
- 宁愿短。写不动就只写清楚“改了哪个文件的哪个函数、会牵连什么”，比空着强得多。
- 文档和代码矛盾时以代码为准，同时把文档改对。

## 2. 变更记录

### 待修：上传等待遮罩失效（写文档时发现，未改代码）

- 现象：桌面端上传文件时，本该出现的等待遮罩没有出现。
- 原因：`source/desktop/page-enhancements.js:282` 查 `.uploading-label`，但 `frontend/src/features/chat/components/Composer.tsx:301` 那个「上传中…」`<span>` 只有内联 style，没这个类名，判断恒为 `false`。
- 修法与取舍见 [07-桌面端与打包.md](07-桌面端与打包.md) §4「已知失效」。改完把这条移到下面并写清改了哪边。

### 2026-08-11 补齐面向 AI 的文档体系

- 改了什么：新增 [00-AI-上手索引.md](00-AI-上手索引.md)（入口与任务路由表）、[06-文件地图.md](06-文件地图.md)（逐文件作用与关键符号）、[07-桌面端与打包.md](07-桌面端与打包.md)（Electron 三层结构、多 API 切换、打包）、本篇；在 04 补上 `/api/code-exec` 两个端点；根 `README.md` 更新文档索引；新增根 `CLAUDE.md` 固化“改代码必须同步改文档”的规则。
- 为什么：原有 01-05 是给人看的教程，缺少「哪个文件干什么、关键函数在哪一行」这类定位信息，AI 每次都得把 5400 行后端和 7300 行前端重读一遍。
- 牵连：纯文档，不动代码。后续所有改动都要按 §1 回填。
- 已更新文档：00、04、06、07、08、README、CLAUDE.md

### 2026-08-11 建立公开仓库

- 改了什么：把源码整理进独立 Git 仓库并推到 GitHub，新增 `README.md`、`LICENSE`（MIT）、`.gitignore`。
- 为什么：原目录是安装后的运行时目录，混着 exe、dll、195MB Electron 发行版和用户数据，不适合直接公开。
- 牵连：`node_modules/`、构建产物、`electron-dist/`、`release/` 安装包、`backend/data/` 用户数据、`.env` 都不入库。克隆后需要自己 `npm install` 和装 Python 依赖。
- 已更新文档：README

### 更早的改动

建仓之前的历史在原目录的 Git 记录里，摘要：

- `f511492` 更新以及问题修复
- `20f90f9` 修复导出菜单遮挡、设置开关点击、DeepSeek 重复注入、图片编辑尺寸
- `f256f6e` bug 修复
- `85fadab` 设置内容分块展示；思考等级与思考强度冲突待处理
