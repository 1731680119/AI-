# 文件地图

每个文件的作用、关键符号（带行号）、改动注意事项。目的是让你不读源码就能定位。

行号是写文档时的位置，改过代码后会偏移，用符号名搜索更可靠。**改了文件请同步更新本篇对应行。**

## 1. 目录总览

```
origin resource/            源码（唯一可改处）
├─ backend/                 Python FastAPI 后端，约 5400 行
│  ├─ routers/              HTTP 路由层，只做校验和分发
│  └─ *.py                  服务层：数据库、模型调用、工具、文件、日志
├─ frontend/                React + TypeScript + Vite，约 7300 行
│  └─ src/
│     ├─ app/               应用入口与初始化
│     ├─ components/layout/ 侧栏、错误边界
│     ├─ features/          按功能分：chat / images / artifacts / settings / projects / diagnostics
│     ├─ services/api/      HTTP 封装，一个后端模块对应一个文件
│     ├─ store/             Zustand 全局状态与业务编排
│     ├─ styles/            CSS，按功能拆分
│     └─ types/             全局 TypeScript 类型
├─ docs/                    本套文档
├─ tools/ui-check/          GUI 检测（连调试端口驱动真实界面，见 §5）
└─ *.bat                    setup / start / check 便捷脚本

../source/                  桌面打包工程
└─ desktop/                 Electron 主进程、preload、注入脚本
```

## 2. 后端文件

### 服务层

| 文件 | 行数 | 作用 | 关键符号 |
| --- | --- | --- | --- |
| `main.py` | 127 | FastAPI 应用装配、生命周期、静态文件挂载 | `lifespan:29`、`create_app:54` |
| `paths.py` | 35 | **所有路径的唯一来源**。数据目录、数据库、上传、图片、日志 | `DATA_DIR:17`（受 `CHATBOT_DATA_DIR` 控制）、`DATABASE_PATH:18`、`FRONTEND_DIST:28`、`ensure_data_dirs:33` |
| `database.py` | 995 | SQLite 全部读写：设置、会话、消息树、项目、记忆、图片、上下文摘要 | 建表 `init_db:37`；设置 `DEFAULT_SETTINGS:198`、`get_settings:270`、`save_settings:432`；风格 `BUILTIN_STYLES:134`、`style_prompt:319`；消息 `add_message:582`、`update_message:614`；树 `get_conversation_tree:651`、`get_active_path:675`、`find_latest_leaf:695`；摘要 `add_context_summary:823`。**1.2.19：`active_image_providers` / `_mirror_active_image_provider` / `_normalize_image_providers` 全删了，`get_settings` 还会从库里 `DELETE` 掉残留的 `image_providers`（不 DELETE 会让保存 422，见 04 §3）** |
| `llm.py` | 609 | 调用 OpenAI 兼容接口，流式解析、思考标签、工具调用循环 | `build_client:16`、`build_api_messages:123`、`stream_chat:435`、`_stream_once:247`、`_ToolCallAssembler:175`、`MAX_TOOL_ROUNDS:172`、`THINKING_LEVELS:236`、`reasoning_effort:239`、`generate_title:575`。三处发请求（`complete_text:30`、`stream_chat`、`generate_title`）都走 `model_params.request_with_healing` |
| `context_service.py` | 216 | 上下文预算与自动压缩：算字符、切分、生成摘要 | `build_context:117`、`message_chars:37`、`_split_point:96`、`IMAGE_COST_CHARS:15`、`SUMMARY_SYSTEM_PROMPT:17` |
| `tools.py` | 137 | 工具总调度：把可用工具的 schema 交给模型，再按名字分发执行 | `available_schemas:112`、`execute:130`、`ToolError:31` |
| `web_search.py` | 522 | 联网搜索，走 DeepSeek Responses API，流式返回进度和来源。配置是多套列表，用哪套由 `search_provider_id` 决定 | `TOOL_SCHEMA:55`、`search:319`、`active_provider:78`、`normalize_provider:96`、`is_configured:113`、`linkify_bare_urls:129`、`endpoint_candidates:156`、`test_provider:457`、`DEFAULT_MODEL:33` |
| `model_probe.py` | 305 | 可用模型检测：读上游 `/models` 清单 + 对单个模型试调一次。不读设置，地址和密钥由调用方直接传 | `list_models:188`、`test_model:233`、`_candidates:49`、`_opener:74`、`_request:90`、`_fetch:98`、`_extract_ids:158`、`ProbeError:41`、`_PATH_MISMATCH_CODES:36`、`_CHAT_MISMATCH_CODES:37`、`TEST_MAX_TOKENS:28`。`test_model` 也走 `model_params.request_with_healing`——否则 GPT-5 系模型会被报成「不可用」，而它只是参数名要换。**`_request` 必须用 `_opener()`**（1.2.17 起）：裸 `urlopen` 会读 Windows 注册表里的系统代理，而发对话的 httpx 只认环境变量，不一致会导致「聊天正常、检测 10061」，见 05 §18。清单为空（`HTTP 200 {"data":[]}`）时**不是地址问题**，是这把密钥的分组没授权模型，1.2.18 起提示语直接这么说 |
| `model_params.py` | 326 | 模型参数兼容层：内置规则打底 + 上游 400 自愈。记账键是「上游地址 + 模型名」，存在 settings 表的 `model_param_quirks` 下 | `request_with_healing:283`、`prepare:216`、`diagnose:251`、`apply_fix:194`、`remember:143`、`forget:167`、`SETTINGS_KEY:31`、`_DROPPABLE:38`、`_REASONING_FAMILY:57`。**`SETTINGS_KEY` 是后端内部键，必须留在 `routers/settings.py` 的 `_INTERNAL_KEYS` 里**，漏了前端保存设置会 422（见 04 §5 settings 表） |
| `memory.py` | 155 | 长期记忆的 `remember` 工具与注入模型的记忆块 | `TOOL_SCHEMA:31`、`build_block:71`（1.2.15 起收 `memory_ids` 做按会话筛选）、`remember:113`、`run_tool:135`、`ITEM_MAX_CHARS:29` |
| `code_exec.py` | 398 | 沙箱跑 Python：静态检查、用户授权、子进程隔离、超时 | `ALLOWED_MODULES:38`、`BANNED_NAMES:48`、`check_code:101`、`run_subprocess:250`、`TIMEOUT_SECONDS:54`、`trust_conversation:150`、`submit_decision:163` |
| `files.py` | 205 | 附件保存与解析（图片、PDF、docx、xlsx、pptx、文本） | `save_upload:37`、`MAX_TEXT_CHARS:34`、`MAX_IMAGE_SIDE:33`、`_parse_pdf:117`、`_parse_xlsx:154` |
| `images.py` | 343 | 图片生成与编辑，多参考图与降级处理 | `_attempts:29`、`_with_failover:40`、`generate_images:148`、`edit_image:187`、`edit_image_with_references:247`、`build_final_prompt`、`save_image_bytes`。**1.2.19 起没有故障转移了**：图片渠道并进「多 API」，`_attempts` 只返回当前生效的那一条；函数名和循环外壳留着是为了少动调用点，**别再把多渠道轮询加回来** |
| `image_export.py` | 251 | 把已保存的图片转成别的格式（TIFF/PDF/HEIC/AVIF 等）。格式清单是**运行时探测**的，不是写死的表 | `_FORMAT_TABLE:37`、`available_formats:115`（带 `lru_cache`）、`convert:178`、`ExportError:103`、`TIFF_COMPRESSION:93`、`MAX_EDGE:99`、`DEFAULT_DPI:100` |
| `logging_config.py` | 641 | 结构化日志、脱敏、环形缓冲、实时订阅、按天轮转 | `setup_logging:572`、`log_event:455`、`redact:108`、`mask_secret:104`、`LogWriter:173`、`subscribe:373`、`ingest:500`、`RETENTION_DAYS:34` |
| `desktop_server.py` | 55 | 桌面模式下的后端启动入口（被 Electron 拉起） | `main:47` |
| `launcher.py` | 218 | 托盘启动器（旧的独立运行方式，桌面版走 Electron） | `TrayLauncher:26`、`main:189` |

### 路由层

路由只做参数校验和分发，业务在服务层。新增路由记得在 `routers/__init__.py:26` 注册。

| 文件 | 前缀 | 端点 |
| --- | --- | --- |
| `routers/settings.py` | `/api/settings` | `GET:65` `PUT:70` `POST /search-test:99` `POST /model-list:136` `POST /model-test:144` |
| `routers/conversations.py` | `/api/conversations` | 列表`41` 搜索`59` 新建`78` 详情`91` 重命名`99` 删除`105` 归项目`111` 换风格`121` 选记忆`129` 切分支`142` |
| `routers/chat.py` | `/api` | `POST /chat:88`（SSE 流，最核心的一条） |
| `routers/uploads.py` | `/api` | `POST /upload:21` |
| `routers/projects.py` | `/api/projects` | 列表`23` 新建`28` 详情`34` 修改`42` 删除`51` |
| `routers/memories.py` | `/api/memories` | 列表`23` 新增`31` 修改`44` 删除`54` 清空`62` |
| `routers/images.py` | `/api/images` | 列表`59` 生成`64` 编辑`83` 删除`148` 导出格式清单`168` 导出`174` 取文件`198`（`_require_image_channel` 是生成/编辑共用的 400 前置检查） |
| `routers/code_exec.py` | `/api/code-exec` | 授权结果`27` 撤销信任`37` |
| `routers/diagnostics.py` | `/api/diagnostics` | 上报`48` 查日志`58` 实时流`76` 详细开关`111/116` 环境`125` 日志目录`131` 导出包`197` 崩溃`298/303/332` |
| `routers/migration.py` | `/api` | 导入旧数据`19`、导入旧图片`47` |

## 3. 前端文件

### 入口与布局

| 文件 | 行数 | 作用 | 关键符号 |
| --- | --- | --- | --- |
| `src/main.tsx` | 24 | React 挂载点，装诊断钩子，全局拦掉窗口级 drag/drop | — |
| `src/app/App.tsx` | 85 | 顶层布局，按 `mode` 切换聊天页/图片页，挂各种弹窗 | `App:15` |
| `src/app/useInitializeApp.ts` | 46 | 启动时拉设置、会话、项目 | `useInitializeApp:13` |
| `src/components/layout/Sidebar.tsx` | 322 | 会话列表、搜索、项目筛选、底部入口 | `Sidebar:8` |
| `src/components/layout/ErrorBoundary.tsx` | 50 | 兜住渲染异常并上报 | `ErrorBoundary:14` |

### 状态与类型（改前端先看这两个）

| 文件 | 行数 | 作用 | 关键符号 |
| --- | --- | --- | --- |
| `src/store/useAppStore.ts` | 613 | **全站状态与业务编排**，Zustand。所有跨组件的流程都在这里 | 类型 `Store:40`；核心动作 `sendMessage:84`、`stopStreaming:85`、`switchBranch:86`、`refreshTree:83`、`answerCodeExec:134`、`saveSettings:89`、`generateImages`/`editImage`、`assignProject:103`、`assignStyle:105`、`setThinkingLevel:161`；`chatApiId`/`setChatApiId`（1.2.16，当前选中的聊天渠道）、`imageApiId`/`setImageApiId`（1.2.19，绘画渠道）——两者都**只在内存里**，发请求时作为 `api_id` 带上 |
| `src/types/index.ts` | 304 | 全局类型，前后端字段的契约 | `Message:70`、`ToolCall:40`、`Attachment:5`、`Settings:156`、`ConversationTree:128`、`ConversationMeta:106`、`Project:86`、`ChatStyle:99`、`Memory:141`、`ImageRecord:234`、`ContextCompactInfo:222`、`CodeExecRequest:29`、`AppMode:231`。**1.2.19 删掉了 `ImageProvider` 和 `Settings.image_providers`** |

改字段的顺序：后端 `DEFAULT_SETTINGS` / 表结构 → `types/index.ts` → store → 组件。漏掉 `types` 会在 `npm run build` 时报类型错，这是好事，别用 `any` 绕。

### 功能模块

| 文件 | 行数 | 作用 |
| --- | --- | --- |
| `features/chat/components/Composer.tsx` | 514 | 输入框、附件（点选/粘贴/拖拽）、模型/风格/思考等级/记忆选择、发送与停止。记忆按钮是拆分式的：左半开关、右半 ▾ 展开逐条勾选（1.2.15）。模型菜单 1.2.16 起是「渠道 → 模型」两级（桌面端走 `useDesktopChannels`，浏览器退回 `settings.models`），选中结果拆成 `default_model` + store 里的 `chatApiId` |
| `features/chat/components/MessageList.tsx` | 276 | 消息渲染、分支切换、编辑重发、重新生成 |
| `features/chat/components/Markdown.tsx` | 125 | Markdown 渲染与代码块（`Markdown:88`，已 memo，注意性能） |
| `features/chat/components/ToolCallBlock.tsx` | 149 | 工具调用过程展示（搜索进度、来源、代码执行） |
| `features/chat/components/CodeExecConfirm.tsx` | 112 | 跑代码前的授权弹窗 |
| `features/chat/components/ExportMenu.tsx` | 45 | 导出菜单，**顶栏菜单的作用域样式范例** |
| `features/chat/exportConversation.ts` | 89 | 导出 Markdown / JSON（`toMarkdown:41`、`toJson:68`、`activePathOf:7`） |
| `features/chat/attachmentIcon.tsx` | 20 | 附件类型图标 |
| `hooks/useFileDrop.ts` | 78 | 拖拽投放区通用钩子，聊天输入框和图片编辑两个区共用（`useFileDrop:27`） |
| `hooks/useUpdateState.ts` | 95 | 订阅主进程推来的自动更新状态，导出 `state / supported / hasUpdate / check / download / install`。浏览器里没有桥，状态恒为 `disabled`。`state.channel` 是本次走通的线路 |
| `hooks/useSortableList.ts` | 226 | 指针拖动排序（缩小跟随 + 原位占位 + 相邻项让位）。给 `useSortableList(count, onReorder, { handleSelector })`，用 `containerRef` + `itemProps(i)` 挂上；`handleSelector`（1.2.15 起）限定只有点在把手上才起拖，判断在 `preventDefault()` 之前，否则卡片里的输入框会点不进去。注入脚本里有等价的 `makeSortable`，**两处要一起改** |
| `hooks/useDesktopChannels.ts` | 81 | 1.2.16 新增。读桌面端的渠道列表（含每条渠道自带的模型清单），滤掉「没启用 / 没地址 / 没密钥」的；`groupByChannel(channels, capability)` 按用途摊成「渠道 → 模型」。**不做订阅**——模型清单由注入的设置面板维护，改完不通知 React，所以暴露 `refresh` 让调用方在打开菜单时重拉。浏览器里 `supported` 恒为 false，调用方退回 `settings.models` |
| `features/artifacts/artifactVersions.ts` | 80 | 从消息里抽 html/svg 代码块并做版本管理（`extractBlocks:28`、`collectVersions:62`、`ARTIFACT_LANGS:5`） |
| `features/artifacts/components/ArtifactPanel.tsx` | 157 | 代码预览面板 |
| `features/images/components/ImagePage.tsx` | 483 | 图片生成/编辑页，参考图、尺寸、历史、表单重置、拖拽上传。详情视图里每张图带「导出 / 下载原图」两个按钮（`.image-card-actions`），生成结果和历史记录共用这一个视图。**1.2.19 起表单里多一个和聊天输入框同款的模型选择框**（`useDesktopChannels` + `groupByChannel(channels, 'image')`），生成和编辑两个 tab 共用它 |
| `features/images/components/ExportDialog.tsx` | 252 | 导出弹窗：格式清单向后端要，按格式显示质量/TIFF 压缩/DPI/底色/尺寸控件。保存优先走 `chatbotDesktop.saveBinaryFile`（原生保存对话框），没有桥才回退 `<a download>` |
| `features/settings/components/SettingsModal.tsx` | 958 | 设置弹窗，最大的组件；桌面端注入区块也挂在这里。搜索配置的「检测可用模型」入口走 `singleSource`。1.2.12 起多一栏「关于与更新」；弹窗本身可最大化、可拖三个把手改尺寸（`maximized`、`modalSize`、`startResize`，每次打开都重置）；**1.2.18 删掉了「聊天」栏的模型列表**（连同 `chatSources`、`applyPickedModels`）；**1.2.19 删掉了整块图片渠道 UI 和图片质量下拉**，「图片生成」栏只剩「图片尺寸」一项，见 08 §1.2.19 ②；**同版本加了「改了没保存就关掉」的确认框**（`isDirty()`、`confirmClose`、`closeBridge()`），主窗口的叉也会转到这里问，见 07 §5.0 |
| `features/settings/components/UpdatePanel.tsx` | 157 | 「关于与更新」面板：版本号、运行环境、更新状态、进度条、检查/下载/退出并安装。Release 正文按**纯文本**渲染（`<pre>`），故意不引 Markdown 渲染器——那是网络内容 |
| `features/settings/components/ModelPicker.tsx` | 215 | 模型选择器：读上游清单勾选，逐个「测试」。密钥用 `ModelSource.resolveKey()` 回调懒取，桌面端多 API 的密钥不必提前解密 |
| `features/settings/components/MemoryPanel.tsx` | 153 | 长期记忆管理 |
| `features/projects/components/ProjectModal.tsx` | 95 | 项目新建/编辑 |
| `features/diagnostics/components/DiagnosticsPanel.tsx` | 335 | 日志面板、实时流、导出诊断包、打开诊断文件夹（`openDiagnosticsFolder`） |

### API 层

`services/api/` 一个文件对应一个后端模块，函数名基本等于用途，改接口时对照 [04-接口与数据字典.md](04-接口与数据字典.md)。

| 文件 | 说明 |
| --- | --- |
| `http.ts` | 统一请求与错误处理（`requestJson:63`、`requestVoid:70`），所有请求都应走它。二进制走 `requestBlob:96`，文件名由 `parseFilename:81` 从 `Content-Disposition` 里取（优先 RFC 5987 的 `filename*`） |
| `base.ts` | `API_BASE = '/api'`，开发模式由 Vite 代理到 8010 |
| `chat.ts` | SSE 流式聊天（`streamChat:37`、`ChatEvent:4`、`ChatRequest:20`）。**改流式协议必须同时改后端 `llm.py`** |
| `conversations.ts` | 会话增删改查、搜索、切分支、导入旧数据 |
| `settings.ts` / `projects.ts` / `memories.ts` / `uploads.ts` / `codeExec.ts` | 对应模块的薄封装 |
| `images.ts` | 图片生成、编辑、删除、文件 URL（`imageFileUrl:56`） |
| `diagnostics.ts` | 日志查询、实时流（`streamLogs:90`）、系统信息、崩溃记录 |
| `services/diagnostics.ts` | 前端自己的日志上报（`log:60`、`logError:91`、`installDiagnostics:144`、`flush:107`） |

### 样式

按功能拆开，`index.css:13` 负责引入顺序，顺序影响覆盖关系。

| 文件 | 行数 | 范围 |
| --- | --- | --- |
| `base.css` | 104 | 变量、重置、主色。**改主题色改这里** |
| `chat.css` | 565 | 聊天区、输入框、`.composer.drag-over` 拖拽高亮、`.model-menu`（向上弹出，见 00 §3） |
| `shared-components.css` | 637 | 复用组件与**作用域覆盖**（顶栏菜单、模型选择器里的勾选框都照这里加） |
| `sidebar.css` | 249 | 侧栏 |
| `settings.css` | 303 | 设置弹窗；1.2.19 起多一个 `.confirm-dirty` 未保存确认框（`position: fixed`，不叠第二层遮罩） |
| `images.css` | 486 | 图片页，含两个投放区的 `.drag-over` 样式；`.image-model-select` 是模型选择框的作用域覆盖（1.2.19，不动公共的 `.model-menu`，见 CLAUDE.md §5） |
| `artifacts.css` | 139 | 预览面板 |
| `diagnostics.css` | 221 | 日志面板 |

## 4. 桌面端文件

细节见 [07-桌面端与打包.md](07-桌面端与打包.md)。

| 文件 | 行数 | 作用 |
| --- | --- | --- |
| `../source/desktop/main.cjs` | 1370 | 主进程：拉起后端、开窗、托盘、渠道解析（`apiAttemptPlan` 只返回一条，不再轮换）、DeepSeek 标签页、崩溃上报与诊断文件夹（`promptLastRunCrashed`）、导出图片的原生保存对话框（`desktop:save-binary`）、更新状态广播（`broadcastUpdateState`、`prepareQuitForUpdate`）。**1.2.19：`beginApiAttempt` 多收一个 `target`（chat/image），由 `apiSettingsPatch()` 决定注入 `base_url` 还是 `image_base_url`**；同版本 `handleWindowClose` 会先看 `settingsDirtyWindows`，脏就把关窗问题转给页面（07 §5.0） |
| `../source/desktop/preload.cjs` | 55 | 唯一的 IPC 桥，暴露 `window.chatbotDesktop`（含 `saveBinaryFile`、`getUpdateState`、`checkForUpdate`、`downloadUpdate`、`installUpdate`、`onUpdateState`；1.2.19 加 `setSettingsDirty`、`onCloseRequested`、`resolveClose`） |
| `../source/desktop/updater.cjs` | 274 | 包装 `electron-updater`：**启动 8 秒后静默查一次，之后只认手动**（`autoCheckDone` 把关，没有定时器）。check/download 前先 `openChannel()` 走降级探测，`finally` 里一定 `teardown()`。provider 是 generic 不是 github（后者会自己拼 `api.github.com`，镜像就用不上了）。未打包时直接 `disabled` |
| `../source/desktop/net-fallback.cjs` | 389 | 更新通道降级：`establish()` 依次试直连 → DoH 解析后直连 → 镜像站 → HTTP 代理，`teardown()` 还原。**所有改动只在本进程内存里**，不写 hosts、不装驱动、不改系统代理，进程一死就没了。为什么不用 Steamcommunity_302 见 08 §3 |
| `../source/desktop/splash.cjs` | 103 | 启动画面窗口（`create` / `setStatus` / `close`）。在 `startBackend()` 之前就要显示，所以 `loadFile` 本地 html 而不走后端。所有方法都吞异常——它崩了不能带崩启动流程 |
| `../source/desktop/splash.html` | 158 | 启动画面的页面本体，零外部依赖。主进程用 `executeJavaScript` 调它的 `setSplashStatus` / `setSplashVersion` |
| `../source/desktop/page-enhancements.js` | 1213 | 注入页面的桌面端专属 UI（多 API 列表、每条渠道的模型清单、右键菜单、等待提示、失败提示）。拖动排序是 `makeSortable()`（指针事件自实现，不是 HTML5 drag&drop），和前端的 `useSortableList` 是等价逻辑，**改效果要两处一起改**。1.2.15 起 API 卡片默认折叠，展开状态存在 `injectSettings` 闭包的 `expandedIds` 里，**故意不持久化**；1.2.16 起卡片里多一块模型清单（`buildModelBlock`、`probeCredentials`）、只打一条渠道；1.2.17 起重绘列表**一律走 `renderApiListKeepingScroll()`**，直接调 `renderApiList()` 会让弹窗弹回顶部（见 07 §4.2）；1.2.18 起模型行的状态文字**一律走 `setStatus`**，直接写 `textContent` 会被上游的长报错撑爆行宽；**1.2.19 起 fetch 补丁除 `POST /api/chat` 外还拦 `/api/images/generate` 和 `/api/images/edit`（`runImageRequest`，`target: 'image'`），`imageRequestFields()` 同时认 JSON 和 FormData** |
| `../source/desktop/enhancements.css` | 508 | 注入样式，类名统一 `enh-` 前缀。`.enh-model-option input[type='checkbox']` 是压回 `.field input{width:100%}` 的作用域覆盖，别删。1.2.18 起几个 grid 容器都写死了 `grid-template-columns: minmax(0, 1fr)`——grid 默认列宽是 `auto`（≈max-content），漏了这条长内容就会把卡片撑出屏幕。**1.2.19 补了 `.enh-model-picker[hidden]`、`.enh-model-option[hidden]` 两条 `display: none !important`**——UA 自带的 `[hidden]` 规则优先级最低，会被同选择器上显式写的 `display` 盖掉，导致「收起」按钮点了没反应；新加的 `enh-` 元素凡是写了 `display` 又靠 `hidden` 收起的都得补一条 |
| `../source/desktop/diagnostics-logger.cjs` | 291 | 主进程侧日志，转发到后端 `/api/diagnostics/ingest` |
| `../source/desktop/diagnostics-archive.cjs` | 279 | 崩溃后把 `logDir` 打包成「未处理-…」诊断文件夹（`createArchive:66`、`markProcessed:213`、`cleanup:250`）。**不依赖后端**，细节见 07 §2.1 |
| `../source/desktop/installer.nsh` | — | NSIS 安装脚本片段 |

## 5. GUI 检测工具

用法见 [`../tools/ui-check/README.md`](../tools/ui-check/README.md)。**不参与打包**，纯开发期工具，
改它不需要升版本、不需要重新出安装包。零 npm 依赖，Node 22+ 直接跑。

原理是连应用自己开的 Chrome DevTools 调试端口（`--remote-debugging-port=9222`），
驱动**真实渲染进程**做点击和输入，再截图 + 跑几何断言 + 与基线做像素 diff。
不用 Puppeteer/Playwright 是因为它们要下自己的浏览器内核，那样测的就不是用户实际跑的那个界面了。

| 文件 | 行数 | 作用 |
| --- | --- | --- |
| `../tools/ui-check/run.mjs` | 359 | 入口与编排：起应用、遍历「分辨率×主题」矩阵、逐场景跑断言截图、出报告。`shutdown()` 必须走优雅退出，硬杀会留下孤儿后端进程和假的「未处理-」诊断文件夹 |
| `../tools/ui-check/cdp.mjs` | 257 | 极简 CDP 客户端。`clickTarget()` 用 `Input.dispatchMouseEvent` 而不是 `el.click()`——后者绕过命中测试，被遮住也能"点到"，而那正是要查的问题 |
| `../tools/ui-check/rules.mjs` | 223 | 几何断言规则，整段注入页面执行。7 条规则，每条对应一个真实踩过的坑（见 05 §5 / CLAUDE.md §5） |
| `../tools/ui-check/scenes.mjs` | 205 | 界面场景定义 + `RESET_STEPS`。**所有场景必须只读**：这套东西对着真实 `user-data/` 跑 |
| `../tools/ui-check/diff.mjs` | 88 | 像素 diff。借页面自己的 Canvas 做，所以不用装 pngjs/pixelmatch |
| `../tools/ui-check/report.mjs` | 134 | 生成自带样式的 HTML 报告 |
| `../tools/ui-check/baseline/`、`report/` | — | 本机产物，已 gitignore。基线**不能跨机器共用**（字体渲染和 DPI 不同会全是假差异） |

抓不到的东西：**Electron 主进程的原生 UI**（窗口菜单、`dialog.showErrorBox`、托盘、
系统通知、NSIS 安装界面）不在渲染进程里，CDP 看不见，只能靠代码审查或系统级截屏。

